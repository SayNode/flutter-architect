TODO:

- edit build.gradle on new project creation
- force flag for commands to re-generate files
- crashlytics
- add CODEWONERS file

Theme:
- init theme and storage in main
- add theme in GetMaterialApp

- fix secure storage not being added if you allready have shared and you try to add both and viceversa

 Storage:
 -- add warning in console if flags are missing

Add prompt for custom bundle id